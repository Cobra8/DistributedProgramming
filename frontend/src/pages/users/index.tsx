import { Box } from "@chakra-ui/layout";
import { Frontend } from "../../components/Frontend";

const Ping = () => {
  return (
    <Frontend siteTitle="Users" variant="wide">
      <Box>some more logic / UI here</Box>
    </Frontend>
  );
};

export default Ping;
